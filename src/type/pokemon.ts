//need id, name image, types, pyscial infor like height and weight and abilities to match the work in recoding video.

export interface Pokemon {
    id: number;
    name: string;
    image: string;
    types: string[];
    height: number;
    weight: number;
    abilities: string[];
  }